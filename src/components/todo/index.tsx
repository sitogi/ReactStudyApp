/** @jsx jsx */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import React, { FC } from 'react';
import { jsx } from '@emotion/core';
import Container from 'components/todo/example';

const TodoList: FC = () => (
  <div>
    <Container />
  </div>
);

export default TodoList;
