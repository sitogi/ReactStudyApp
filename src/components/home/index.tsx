/** @jsx jsx */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import React, { FC } from 'react';
import { jsx } from '@emotion/core';

const Home: FC = () => (
  <div>
    <h1>React (Create React App), Redux, Redux-Saga, TypeScript による学習用アプリです。</h1>
  </div>
);

export default Home;
